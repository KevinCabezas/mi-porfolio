export type TSkillCard = {
    title: string;
    icon: string;
    color: string;
  }

export type TSecctionsSkills = {
    title: string;
    description: string;
    icon: string;
    secctions: TSkillCard[];
  }
